Player class
    NewGame method
        When printing off random numbers the numbers should be between 8 and 20 but they are showing between 9 and 100


Indicators
    // !
private Tile OnQuestMaker(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// ?
private Tile OnQuestionMark(int x, int y, int row, int col, string ascii) => 
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
Walkable Terrain and Areas
PlantLife
    // ,
private Tile OnTree(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// ,
private Tile OnDeadTree(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
Accent Pieces
    // ▪
private Tile OnSmallStone3(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);4
Non walkable Terrain and Areas
// □
private Tile OnObstaclesgridX(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);

Design and Decorations
Containers
     // █
 private Tile OnContainer(int x, int y, int row, int col, string ascii) =>
     new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);

Bow Drawing 
// '
private Tile OnApostrophe(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// ┌, ┐, └, ┘, ─, │, ┼, ┬, ┴, ├, ┤
private Tile OnWallCharacter(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// #, +, -, |, /, \, _ 
private Tile OnSpecialBuilders(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);

UI Decorations and symbols
    private Tile OnUpArrow3(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// v
private Tile OnDownArrow3(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// <
private Tile OnLeftArrow3(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);
// >
private Tile OnRightArrow3(int x, int y, int row, int col, string ascii) =>
    new Tile(x, y, row, col, TileTypes.empty, false, false, new(), ascii);

